info:
  name: Geo Lookup Country
  type: http
  seq: 8

http:
  method: GET
  url: https://localhost/api/ipinfo
  body:
    type: json
    data: |-
      {
        "type": "country",
        "value": "KR"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
