info:
  name: Sample POST Resource
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost/api/resources
  auth: inherit
  headers:
    Content-Type: application/json
  body:
    mode: json
    json: |
      {
        "name": "sample-resource",
        "description": "created from Bruno sample"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
