info:
  name: Sample PATCH Resource
  type: http
  seq: 5

http:
  method: PATCH
  url: https://localhost/api/resources/123
  auth: inherit
  headers:
    Content-Type: application/json
  body:
    mode: json
    json: |
      {
        "description": "partial update via PATCH"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
