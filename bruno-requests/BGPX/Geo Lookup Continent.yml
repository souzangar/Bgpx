info:
  name: Geo Lookup Continent
  type: http
  seq: 9

http:
  method: GET
  url: https://localhost/api/ipinfo
  body:
    type: json
    data: |-
      {
        "type": "continent",
        "value": "EU"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
